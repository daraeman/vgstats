{
  "name": "vg_stats",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "setup": "node server/setup.js",
    "start_update_markets": "node server/index.js start update_markets",
    "stop_update_markets": "node server/index.js stop update_markets",
    "status_update_markets": "node server/index.js status update_markets",
    "start_database": "if [ -z $(pidof mongod) ]; then mongod --config mongo.conf; else echo 'Mongod is already running'; fi;",
    "stop_database": "if [ ! -z $(pidof mongod) ]; then kill $(pidof mongod); else echo 'Mongod was not running'; fi;"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "daemonize2": "^0.4.2",
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "fs.promised": "^3.0.0",
    "mkdirp-promise": "^5.0.1",
    "mongoose": "^4.12.4",
    "react": "^16.0.0",
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5",
    "simple-promise-queue": "^0.1.7"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "nodemon": "^1.12.1"
  }
}
